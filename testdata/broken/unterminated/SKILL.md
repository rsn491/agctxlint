---
name: unterminated
description: A fixture whose front matter is never closed, so the whole file is read as YAML. Expected finding: frontmatter.unterminated.

# Unterminated

There is no closing fence above this heading.
