---
name: verbose-description
description: Use this skill when the description is deliberately far too long to sit in the context window of every session, because the runtime loads a skill's name and description eagerly and only reads the body once the skill is invoked. Use this skill when the description is deliberately far too long to sit in the context window of every session, because the runtime loads a skill's name and description eagerly and only reads the body once the skill is invoked. Use this skill when the description is deliberately far too long to sit in the context window of every session, because the runtime loads a skill's name and description eagerly and only reads the body once the skill is invoked. Use this skill when the description is deliberately far too long to sit in the context window of every session, because the runtime loads a skill's name and description eagerly and only reads the body once the skill is invoked. Use this skill when the description is deliberately far too long to sit in the context window of every session, because the runtime loads a skill's name and description eagerly and only reads the body once the skill is invoked. Use this skill when the description is deliberately far too long to sit in the context window of every session, because the runtime loads a skill's name and description eagerly and only reads the body once the skill is invoked. Use this skill when the description is deliberately far too long to sit in the context window of every session, because the runtime loads a skill's name and description eagerly and only reads the body once the skill is invoked. Use this skill when the description is deliberately far too long to sit in the context window of every session, because the runtime loads a skill's name and description eagerly and only reads the body once the skill is invoked.
---

# Verbose description

Expected findings: description.length and tokens.description.
