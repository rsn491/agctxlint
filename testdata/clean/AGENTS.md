# Example project

Instructions an agent should follow when working in this repository.

## Build

Run `go build ./...` before opening a pull request.

## Tests

Run `go test ./...`. Every new rule needs a table entry covering it.

## Style

Keep comments explaining why a check exists, not what the code does.
